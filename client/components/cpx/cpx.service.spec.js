'use strict';

describe('Service: cpx', function () {

  // load the service's module
  beforeEach(module('cpxApp'));

  // instantiate service
  var cpx;
  beforeEach(inject(function (_cpx_) {
    cpx = _cpx_;
  }));

  it('should do something', function () {
    //TODO
  });

});
